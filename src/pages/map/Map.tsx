import React, { FC } from 'react';

import { Header } from '../../features/header';
import { Content } from '../../features/content';

export const Map: FC = () => {
	return (
		<>
			<Header />
			<Content />
		</>
	);
}
